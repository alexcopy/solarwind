version: '3'

services:
  solarwind:
    image: redcopy/solarwind:latest
    privileged: true
    devices:
      - "/dev/i2c-1"
    volumes:
      - /mnt/secrets:/app/data
      - /mnt/secrets:/app/secrets
